<h1>Your recipes</h1>

<ul>
  <% @recipes.each do |recipe| %>
    <li><%= link_to "#{recipe.name}", recipe_path(recipe) %></li>
  <% end %>
</ul>

<%= simple_form_for(Recipe.new) do |f|%>
    <%= f.input :name %>
    <%= f.submit  %>
  <% end %>
